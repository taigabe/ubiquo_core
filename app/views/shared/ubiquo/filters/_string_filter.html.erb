<div id="string_filter_content">
<% form_tag(url_for_options, :method => :get) do %>
	<%= build_hidden_field_tags(link) %>
	<p><%= text_field_tag options[:field], params[options[:field]] %>
 	<%= submit_tag t('ubiquo.search') %></p>
<% end %>
</div>
